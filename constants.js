const listenerNames = {
  CLIENT_READY_FOR_GAME: "client-ready-for-game",
};

const emitNames = {
  GAME_STARTED: "game-started",
  JOIN_LOBBY: "join-lobby",
  LEAVE_LOBBY: "leave-lobby",
};

module.exports = {
  listenerNames,
  emitNames
}
